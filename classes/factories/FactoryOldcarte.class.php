<?php
	/*
	 * AUTO-GENERATED FILE on 23/02/2017 14:20:58 BY FactoryGenerator.class.php
	 */

abstract class FactoryOldcarte {

	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Oldcarte11]*/	/*[/TAG-Oldcarte11]*/


	public static function getFromTableOldcarte($id=-1) {
		//Generated by FactoryGenerator::generateGetAllFromTable()
		$listeObjet = array();
		// Lancement de la requete
		if (empty(self::$_requete)) {
			$requete = 'SELECT * FROM `oldcarte`';
		} else {
			$requete = self::$_requete;
		}
		if (!is_array($id)) {
			if ($id > -1) {
				$requete .= ' WHERE id = :id';
				//Il faut que le parametre soit dans un array pour le BIND
				$id = array(':id' => $id);
			} else {
				/* Tous les objets ==> il faut les ordonner */
				$requete .= ' ORDER BY id ASC';
			}
		} else {
			$requete .= ' WHERE id IN ('.implode(",",$id).') ORDER BY id ASC';
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($id) || $id > -1) {
			database::getInstance() -> bind($id);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table oldcarte');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		
	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Oldcarte2]*/	/*[/TAG-Oldcarte2]*/

		/* Traitement des donnees */
		foreach ($datas AS $data) {
			/* objet par defaut */
			$listeObjet[] = new Oldcarte($data['id_carte'],$data['x'],$data['y'],$data['vision'],$data['terrain']);
		
	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-getFromTableOldcarte1]*/	/*[/TAG-getFromTableOldcarte1]*/

		}
		if (!empty($listeObjet) && sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}

	public static function getFromExtTableId_carte($id_carte=0) {
		//Generated by FactoryGenerator::generateGetFromTableFromFK()
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `oldcarte` ';
		if ($id_carte == 0) {
			$requete .= 'WHERE id_carte > 0 ORDER BY id ASC';
		} else {
			$requete .= 'WHERE id_carte = :id_carte ORDER BY id ASC';
			//Il faut que le parametre soit dans un array pour le BIND
			$id_carte = array(':id_carte' => $id_carte);
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($id_carte) || $id_carte > 0) {
			database::getInstance() -> bind($id_carte);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table oldcarte');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Oldcarte($data['id_carte'],$data['x'],$data['y'],$data['vision'],$data['terrain']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}

	public static function getFromExtTableTerrain($terrain=0) {
		//Generated by FactoryGenerator::generateGetFromTableFromFK()
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `oldcarte` ';
		if ($terrain == 0) {
			$requete .= 'WHERE terrain > 0 ORDER BY id ASC';
		} else {
			$requete .= 'WHERE terrain = :terrain ORDER BY id ASC';
			//Il faut que le parametre soit dans un array pour le BIND
			$terrain = array(':terrain' => $terrain);
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($terrain) || $terrain > 0) {
			database::getInstance() -> bind($terrain);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table oldcarte');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Oldcarte($data['id_carte'],$data['x'],$data['y'],$data['vision'],$data['terrain']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}


	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Oldcarte21]*/	/*[/TAG-Oldcarte21]*/


}
?>