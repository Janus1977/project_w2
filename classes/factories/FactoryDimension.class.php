<?php
	/*
	 * AUTO-GENERATED FILE on 23/02/2017 14:20:58 BY FactoryGenerator.class.php
	 */

abstract class FactoryDimension {

	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Dimension11]*/	/*[/TAG-Dimension11]*/


	public static function getFromTableDimension($id=-1) {
		//Generated by FactoryGenerator::generateGetAllFromTable()
		$listeObjet = array();
		// Lancement de la requete
		if (empty(self::$_requete)) {
			$requete = 'SELECT * FROM `dimension`';
		} else {
			$requete = self::$_requete;
		}
		if (!is_array($id)) {
			if ($id > -1) {
				$requete .= ' WHERE id = :id';
				//Il faut que le parametre soit dans un array pour le BIND
				$id = array(':id' => $id);
			} else {
				/* Tous les objets ==> il faut les ordonner */
				$requete .= ' ORDER BY id ASC';
			}
		} else {
			$requete .= ' WHERE id IN ('.implode(",",$id).') ORDER BY id ASC';
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($id) || $id > -1) {
			database::getInstance() -> bind($id);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table dimension');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		
	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Dimension2]*/	/*[/TAG-Dimension2]*/

		/* Traitement des donnees */
		foreach ($datas AS $data) {
			/* objet par defaut */
			$listeObjet[] = new Dimension($data['id'],$data['nom'],$data['type'],$data['largeur'],$data['longueur'],$data['surface']);
		
	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-getFromTableDimension1]*/	/*[/TAG-getFromTableDimension1]*/

		}
		if (!empty($listeObjet) && sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}

	public static function getFromExtTableType($type=0) {
		//Generated by FactoryGenerator::generateGetFromTableFromFK()
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `dimension` ';
		if ($type == 0) {
			$requete .= 'WHERE type > 0 ORDER BY id ASC';
		} else {
			$requete .= 'WHERE type = :type ORDER BY id ASC';
			//Il faut que le parametre soit dans un array pour le BIND
			$type = array(':type' => $type);
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($type) || $type > 0) {
			database::getInstance() -> bind($type);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table dimension');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Dimension($data['id'],$data['nom'],$data['type'],$data['largeur'],$data['longueur'],$data['surface']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}


	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Dimension21]*/
	
	public static function getByLargeur($largeur) {
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `dimension` ';
		$requete .= 'WHERE largeur = :largeur';
		//Il faut que le parametre soit dans un array pour le BIND
		$type = array(':largeur' => $largeur);

		database::getInstance() -> prepareRequete($requete);
		if (is_array($type) || $type > 0) {
			database::getInstance() -> bind($type);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table dimension');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Dimension($data['id'],$data['nom'],$data['type'],$data['largeur'],$data['longueur'],$data['surface']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}
	
	public static function getByLongueur($longueur) {
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `dimension` ';
		$requete .= 'WHERE longueur = :longueur';
		//Il faut que le parametre soit dans un array pour le BIND
		$type = array(':longueur' => $longueur);

		database::getInstance() -> prepareRequete($requete);
		if (is_array($type) || $type > 0) {
			database::getInstance() -> bind($type);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table dimension');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Dimension($data['id'],$data['nom'],$data['type'],$data['largeur'],$data['longueur'],$data['surface']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}
	
	public static function getByLangeurAndLongueur($largeur,$longueur) {
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `dimension` ';
		$requete .= 'WHERE largeur = :largeur AND longueur = :longueur';
		//Il faut que le parametre soit dans un array pour le BIND
		$type = array(':largeur' => $largeur,':longueur' => $longueur);

		database::getInstance() -> prepareRequete($requete);
		if (is_array($type) || $type > 0) {
			database::getInstance() -> bind($type);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table dimension');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Dimension($data['id'],$data['nom'],$data['type'],$data['largeur'],$data['longueur'],$data['surface']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}
	
	/*[/TAG-Dimension21]*/


}
?>