<?php
	/*
	 * AUTO-GENERATED FILE on 23/02/2017 14:20:58 BY FactoryGenerator.class.php
	 */

abstract class FactoryArene {

	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Arene11]*/	/*[/TAG-Arene11]*/


	public static function getFromTableArene($id=-1) {
		//Generated by FactoryGenerator::generateGetAllFromTable()
		$listeObjet = array();
		// Lancement de la requete
		if (empty(self::$_requete)) {
			$requete = 'SELECT * FROM `arene`';
		} else {
			$requete = self::$_requete;
		}
		if (!is_array($id)) {
			if ($id > -1) {
				$requete .= ' WHERE id = :id';
				//Il faut que le parametre soit dans un array pour le BIND
				$id = array(':id' => $id);
			} else {
				/* Tous les objets ==> il faut les ordonner */
				$requete .= ' ORDER BY id ASC';
			}
		} else {
			$requete .= ' WHERE id IN ('.implode(",",$id).') ORDER BY id ASC';
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($id) || $id > -1) {
			database::getInstance() -> bind($id);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table arene');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		
	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Arene2]*/	/*[/TAG-Arene2]*/

		/* Traitement des donnees */
		foreach ($datas AS $data) {
			/* objet par defaut */
			$listeObjet[] = new Arene($data['titre'],$data['nb_joueur_min'],$data['nb_joueur_max'],$data['fantassins_min'],$data['fantassins_max'],$data['motorise_min'],$data['motorise_max'],$data['aerien_min'],$data['aerien_max']);
		
	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-getFromTableArene1]*/	/*[/TAG-getFromTableArene1]*/

		}
		if (!empty($listeObjet) && sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}

	public static function getFromExtTableNb_joueur_min($nb_joueur_min=0) {
		//Generated by FactoryGenerator::generateGetFromTableFromFK()
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `arene` ';
		if ($nb_joueur_min == 0) {
			$requete .= 'WHERE nb_joueur_min > 0 ORDER BY id ASC';
		} else {
			$requete .= 'WHERE nb_joueur_min = :nb_joueur_min ORDER BY id ASC';
			//Il faut que le parametre soit dans un array pour le BIND
			$nb_joueur_min = array(':nb_joueur_min' => $nb_joueur_min);
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($nb_joueur_min) || $nb_joueur_min > 0) {
			database::getInstance() -> bind($nb_joueur_min);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table arene');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Arene($data['titre'],$data['nb_joueur_min'],$data['nb_joueur_max'],$data['fantassins_min'],$data['fantassins_max'],$data['motorise_min'],$data['motorise_max'],$data['aerien_min'],$data['aerien_max']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}

	public static function getFromExtTableNb_joueur_max($nb_joueur_max=0) {
		//Generated by FactoryGenerator::generateGetFromTableFromFK()
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `arene` ';
		if ($nb_joueur_max == 0) {
			$requete .= 'WHERE nb_joueur_max > 0 ORDER BY id ASC';
		} else {
			$requete .= 'WHERE nb_joueur_max = :nb_joueur_max ORDER BY id ASC';
			//Il faut que le parametre soit dans un array pour le BIND
			$nb_joueur_max = array(':nb_joueur_max' => $nb_joueur_max);
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($nb_joueur_max) || $nb_joueur_max > 0) {
			database::getInstance() -> bind($nb_joueur_max);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table arene');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Arene($data['titre'],$data['nb_joueur_min'],$data['nb_joueur_max'],$data['fantassins_min'],$data['fantassins_max'],$data['motorise_min'],$data['motorise_max'],$data['aerien_min'],$data['aerien_max']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}

	public static function getFromExtTableFantassins_min($fantassins_min=0) {
		//Generated by FactoryGenerator::generateGetFromTableFromFK()
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `arene` ';
		if ($fantassins_min == 0) {
			$requete .= 'WHERE fantassins_min > 0 ORDER BY id ASC';
		} else {
			$requete .= 'WHERE fantassins_min = :fantassins_min ORDER BY id ASC';
			//Il faut que le parametre soit dans un array pour le BIND
			$fantassins_min = array(':fantassins_min' => $fantassins_min);
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($fantassins_min) || $fantassins_min > 0) {
			database::getInstance() -> bind($fantassins_min);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table arene');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Arene($data['titre'],$data['nb_joueur_min'],$data['nb_joueur_max'],$data['fantassins_min'],$data['fantassins_max'],$data['motorise_min'],$data['motorise_max'],$data['aerien_min'],$data['aerien_max']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}

	public static function getFromExtTableFantassins_max($fantassins_max=0) {
		//Generated by FactoryGenerator::generateGetFromTableFromFK()
		$listeObjet = array();
		// Lancement de la requete
		$requete = 'SELECT * FROM `arene` ';
		if ($fantassins_max == 0) {
			$requete .= 'WHERE fantassins_max > 0 ORDER BY id ASC';
		} else {
			$requete .= 'WHERE fantassins_max = :fantassins_max ORDER BY id ASC';
			//Il faut que le parametre soit dans un array pour le BIND
			$fantassins_max = array(':fantassins_max' => $fantassins_max);
		}
		database::getInstance() -> prepareRequete($requete);
		if (is_array($fantassins_max) || $fantassins_max > 0) {
			database::getInstance() -> bind($fantassins_max);
		}
		if (! database::getInstance() -> executeRequete()) {
			throw new Exception(__CLASS__.'::'.__FUNCTION__.'(): Impossible de lire la table arene');
		}
		// Recuperation des donnees
		$datas = database::getInstance() -> getTableauResultat();
		// Traitement des donnees
		foreach ($datas AS $data) {
			$listeObjet[] = new Arene($data['titre'],$data['nb_joueur_min'],$data['nb_joueur_max'],$data['fantassins_min'],$data['fantassins_max'],$data['motorise_min'],$data['motorise_max'],$data['aerien_min'],$data['aerien_max']);
		}
		if (sizeof($listeObjet) == 1) {
			$listeObjet = $listeObjet[0];
		}
		return $listeObjet;
	}


	/*
	 * Entre ces deux balises vous pourrez mettre votre code specifique a la classe.
	 * Il sera preserve lors de la reconstruction automatique.
	 */
	/*[TAG-Arene21]*/	/*[/TAG-Arene21]*/


}
?>